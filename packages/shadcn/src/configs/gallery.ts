import type { AppConfig } from "./types";

export const config: AppConfig = {
  key: "gallery",
  title: "Component Gallery",
  placeholder: "Render the shadcn component gallery.",
  loadingText: "Rendering the gallery...",
};
