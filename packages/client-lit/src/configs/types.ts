export interface AppConfig {
  key: string;
  title: string;
  background?: string;
  heroImage?: string;
  heroImageDark?: string;
  placeholder: string;
  loadingText?: string[];
  serverUrl?: string;
}
